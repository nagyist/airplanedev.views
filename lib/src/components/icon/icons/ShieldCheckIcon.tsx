// Code generated via yarn gen:icons. DO NOT EDIT

/* eslint-disable react/display-name */
import { ShieldCheckIcon as HeroShieldCheckIconMini } from "@heroicons/react/20/solid";
import { ShieldCheckIcon as HeroShieldCheckIconOutline } from "@heroicons/react/24/outline";
import { ShieldCheckIcon as HeroShieldCheckIconSolid } from "@heroicons/react/24/solid";
import { forwardRef, Ref } from "react";

import { Icon } from "../Icon";
import { Props } from "../Icon.types";

export const ShieldCheckIconOutline = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroShieldCheckIconOutline />
      </Icon>
    );
  },
);

export const ShieldCheckIconSolid = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroShieldCheckIconSolid />
      </Icon>
    );
  },
);

export const ShieldCheckIconMini = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroShieldCheckIconMini />
      </Icon>
    );
  },
);
