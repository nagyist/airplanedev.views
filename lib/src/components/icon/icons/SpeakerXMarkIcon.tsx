// Code generated via yarn gen:icons. DO NOT EDIT

/* eslint-disable react/display-name */
import { SpeakerXMarkIcon as HeroSpeakerXMarkIconMini } from "@heroicons/react/20/solid";
import { SpeakerXMarkIcon as HeroSpeakerXMarkIconOutline } from "@heroicons/react/24/outline";
import { SpeakerXMarkIcon as HeroSpeakerXMarkIconSolid } from "@heroicons/react/24/solid";
import { forwardRef, Ref } from "react";

import { Icon } from "../Icon";
import { Props } from "../Icon.types";

export const SpeakerXMarkIconOutline = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroSpeakerXMarkIconOutline />
      </Icon>
    );
  },
);

export const SpeakerXMarkIconSolid = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroSpeakerXMarkIconSolid />
      </Icon>
    );
  },
);

export const SpeakerXMarkIconMini = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroSpeakerXMarkIconMini />
      </Icon>
    );
  },
);
