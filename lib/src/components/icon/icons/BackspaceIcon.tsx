// Code generated via yarn gen:icons. DO NOT EDIT

/* eslint-disable react/display-name */
import { BackspaceIcon as HeroBackspaceIconMini } from "@heroicons/react/20/solid";
import { BackspaceIcon as HeroBackspaceIconOutline } from "@heroicons/react/24/outline";
import { BackspaceIcon as HeroBackspaceIconSolid } from "@heroicons/react/24/solid";
import { forwardRef, Ref } from "react";

import { Icon } from "../Icon";
import { Props } from "../Icon.types";

export const BackspaceIconOutline = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroBackspaceIconOutline />
      </Icon>
    );
  },
);

export const BackspaceIconSolid = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroBackspaceIconSolid />
      </Icon>
    );
  },
);

export const BackspaceIconMini = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroBackspaceIconMini />
      </Icon>
    );
  },
);
