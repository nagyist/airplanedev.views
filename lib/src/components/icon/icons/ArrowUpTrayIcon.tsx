// Code generated via yarn gen:icons. DO NOT EDIT

/* eslint-disable react/display-name */
import { ArrowUpTrayIcon as HeroArrowUpTrayIconMini } from "@heroicons/react/20/solid";
import { ArrowUpTrayIcon as HeroArrowUpTrayIconOutline } from "@heroicons/react/24/outline";
import { ArrowUpTrayIcon as HeroArrowUpTrayIconSolid } from "@heroicons/react/24/solid";
import { forwardRef, Ref } from "react";

import { Icon } from "../Icon";
import { Props } from "../Icon.types";

export const ArrowUpTrayIconOutline = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroArrowUpTrayIconOutline />
      </Icon>
    );
  },
);

export const ArrowUpTrayIconSolid = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroArrowUpTrayIconSolid />
      </Icon>
    );
  },
);

export const ArrowUpTrayIconMini = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroArrowUpTrayIconMini />
      </Icon>
    );
  },
);
