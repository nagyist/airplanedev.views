// Code generated via yarn gen:icons. DO NOT EDIT

/* eslint-disable react/display-name */
import { PencilIcon as HeroPencilIconMini } from "@heroicons/react/20/solid";
import { PencilIcon as HeroPencilIconOutline } from "@heroicons/react/24/outline";
import { PencilIcon as HeroPencilIconSolid } from "@heroicons/react/24/solid";
import { forwardRef, Ref } from "react";

import { Icon } from "../Icon";
import { Props } from "../Icon.types";

export const PencilIconOutline = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroPencilIconOutline />
      </Icon>
    );
  },
);

export const PencilIconSolid = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroPencilIconSolid />
      </Icon>
    );
  },
);

export const PencilIconMini = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroPencilIconMini />
      </Icon>
    );
  },
);
