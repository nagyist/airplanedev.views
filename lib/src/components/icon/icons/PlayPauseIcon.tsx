// Code generated via yarn gen:icons. DO NOT EDIT

/* eslint-disable react/display-name */
import { PlayPauseIcon as HeroPlayPauseIconMini } from "@heroicons/react/20/solid";
import { PlayPauseIcon as HeroPlayPauseIconOutline } from "@heroicons/react/24/outline";
import { PlayPauseIcon as HeroPlayPauseIconSolid } from "@heroicons/react/24/solid";
import { forwardRef, Ref } from "react";

import { Icon } from "../Icon";
import { Props } from "../Icon.types";

export const PlayPauseIconOutline = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroPlayPauseIconOutline />
      </Icon>
    );
  },
);

export const PlayPauseIconSolid = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroPlayPauseIconSolid />
      </Icon>
    );
  },
);

export const PlayPauseIconMini = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroPlayPauseIconMini />
      </Icon>
    );
  },
);
