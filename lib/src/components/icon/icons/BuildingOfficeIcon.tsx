// Code generated via yarn gen:icons. DO NOT EDIT

/* eslint-disable react/display-name */
import { BuildingOfficeIcon as HeroBuildingOfficeIconMini } from "@heroicons/react/20/solid";
import { BuildingOfficeIcon as HeroBuildingOfficeIconOutline } from "@heroicons/react/24/outline";
import { BuildingOfficeIcon as HeroBuildingOfficeIconSolid } from "@heroicons/react/24/solid";
import { forwardRef, Ref } from "react";

import { Icon } from "../Icon";
import { Props } from "../Icon.types";

export const BuildingOfficeIconOutline = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroBuildingOfficeIconOutline />
      </Icon>
    );
  },
);

export const BuildingOfficeIconSolid = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroBuildingOfficeIconSolid />
      </Icon>
    );
  },
);

export const BuildingOfficeIconMini = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroBuildingOfficeIconMini />
      </Icon>
    );
  },
);
