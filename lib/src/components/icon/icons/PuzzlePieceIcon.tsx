// Code generated via yarn gen:icons. DO NOT EDIT

/* eslint-disable react/display-name */
import { PuzzlePieceIcon as HeroPuzzlePieceIconMini } from "@heroicons/react/20/solid";
import { PuzzlePieceIcon as HeroPuzzlePieceIconOutline } from "@heroicons/react/24/outline";
import { PuzzlePieceIcon as HeroPuzzlePieceIconSolid } from "@heroicons/react/24/solid";
import { forwardRef, Ref } from "react";

import { Icon } from "../Icon";
import { Props } from "../Icon.types";

export const PuzzlePieceIconOutline = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroPuzzlePieceIconOutline />
      </Icon>
    );
  },
);

export const PuzzlePieceIconSolid = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroPuzzlePieceIconSolid />
      </Icon>
    );
  },
);

export const PuzzlePieceIconMini = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroPuzzlePieceIconMini />
      </Icon>
    );
  },
);
