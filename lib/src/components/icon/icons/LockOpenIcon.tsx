// Code generated via yarn gen:icons. DO NOT EDIT

/* eslint-disable react/display-name */
import { LockOpenIcon as HeroLockOpenIconMini } from "@heroicons/react/20/solid";
import { LockOpenIcon as HeroLockOpenIconOutline } from "@heroicons/react/24/outline";
import { LockOpenIcon as HeroLockOpenIconSolid } from "@heroicons/react/24/solid";
import { forwardRef, Ref } from "react";

import { Icon } from "../Icon";
import { Props } from "../Icon.types";

export const LockOpenIconOutline = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroLockOpenIconOutline />
      </Icon>
    );
  },
);

export const LockOpenIconSolid = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroLockOpenIconSolid />
      </Icon>
    );
  },
);

export const LockOpenIconMini = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroLockOpenIconMini />
      </Icon>
    );
  },
);
