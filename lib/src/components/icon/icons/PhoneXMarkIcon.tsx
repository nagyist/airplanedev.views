// Code generated via yarn gen:icons. DO NOT EDIT

/* eslint-disable react/display-name */
import { PhoneXMarkIcon as HeroPhoneXMarkIconMini } from "@heroicons/react/20/solid";
import { PhoneXMarkIcon as HeroPhoneXMarkIconOutline } from "@heroicons/react/24/outline";
import { PhoneXMarkIcon as HeroPhoneXMarkIconSolid } from "@heroicons/react/24/solid";
import { forwardRef, Ref } from "react";

import { Icon } from "../Icon";
import { Props } from "../Icon.types";

export const PhoneXMarkIconOutline = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroPhoneXMarkIconOutline />
      </Icon>
    );
  },
);

export const PhoneXMarkIconSolid = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroPhoneXMarkIconSolid />
      </Icon>
    );
  },
);

export const PhoneXMarkIconMini = forwardRef(
  (props: Props, ref: Ref<SVGSVGElement>) => {
    return (
      <Icon {...props} ref={ref}>
        <HeroPhoneXMarkIconMini />
      </Icon>
    );
  },
);
